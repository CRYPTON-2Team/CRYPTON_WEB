export interface SigninType {
    email: string;
    password: string;
}

export interface SignpupType {
    email: string;
    authCode: string;
    name: string;
    birth: string;
    id: string;
    password: string;
    passwordCheck: string;
}